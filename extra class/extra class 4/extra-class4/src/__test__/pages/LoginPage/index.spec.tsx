import LoginPage from '@/components/pages/LoginPage';
import { fireEvent, render } from '@testing-library/react';

describe('LoginPage component test', () => {
    it('should render', () => render(<LoginPage/>))
})